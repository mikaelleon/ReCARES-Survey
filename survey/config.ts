/**
 * Perimeter-adjacent phases unlock the extended security tier.
 * Keep this list as config so it can change without a code deploy later.
 *
 * TODO (team): Placeholder phase list — confirm the real phases with the HOA
 * before go-live, then update this array to match.
 */
export const PERIMETER_PHASES: string[] = ['Phase 1', 'Phase 4'];
